# 项目结构优化方案

## 📊 当前结构分析

### 发现的问题

1. **Chat 组件位置不一致**
   - `src/components/chat/` - UI 组件（ChatContainer, ChatInput, ChatMessage）
   - `src/features/chat/` - 逻辑层（hooks, store, types）
   - **问题**：相关功能分散在两个目录

2. **Providers 位置**
   - `src/components/providers/` - I18nProviderWrapper, StoreProvider
   - **问题**：Providers 更像是基础设施，不是 UI 组件

3. **组件组织不一致**
   - `i18n` 的组件在 `features/i18n/components/`
   - `chat` 的组件在 `components/chat/`
   - **问题**：组织方式不统一

## ✅ 优化方案

### 方案 1：将 Chat 组件移动到 features/chat/components/

**目标**：让聊天功能的所有代码集中在一起

```
src/features/chat/
├── components/        # ✅ 新增：聊天 UI 组件
│   ├── ChatContainer.tsx
│   ├── ChatInput.tsx
│   ├── ChatMessage.tsx
│   └── index.ts
├── hooks/
│   └── useChat.ts
├── store/
│   └── chatStore.ts
└── types.ts
```

**优点**：
- ✅ 聊天功能完全集中
- ✅ 与 i18n 的组织方式一致
- ✅ 便于功能模块的独立开发和维护

### 方案 2：将 Providers 移动到 shared/providers/

**目标**：Providers 是基础设施，应该放在 shared/

```
src/shared/
├── providers/         # ✅ 新增：全局 Providers
│   ├── I18nProviderWrapper.tsx
│   ├── StoreProvider.tsx
│   └── index.ts
├── hooks/
├── utils/
└── ...
```

**优点**：
- ✅ Providers 是基础设施，不是业务组件
- ✅ 与 shared 的职责一致

### 方案 3：components/ 只保留 UI 基础组件

**目标**：components/ 只保留 shadcn/ui 等基础组件

```
src/components/
└── ui/                # 只保留 UI 基础组件
    ├── button.tsx
    ├── input.tsx
    └── scroll-area.tsx
```

**优点**：
- ✅ 职责更清晰
- ✅ 业务组件在各自的功能模块下

## 🎯 推荐优化步骤

### 步骤 1：移动 Chat 组件
1. 创建 `src/features/chat/components/`
2. 移动 `src/components/chat/` → `src/features/chat/components/`
3. 更新导入路径

### 步骤 2：移动 Providers
1. 创建 `src/shared/providers/`
2. 移动 `src/components/providers/` → `src/shared/providers/`
3. 更新导入路径

### 步骤 3：清理 components/
1. 删除空的 `components/chat/` 和 `components/providers/`
2. 只保留 `components/ui/`

## 📝 优化后的结构

```
src/
├── app/               # Next.js 路由
├── components/        # 只保留 UI 基础组件
│   └── ui/
├── domains/          # 业务领域
│   └── users/
├── features/         # 技术功能（完整功能模块）
│   ├── chat/         # 聊天功能（包含组件）
│   │   ├── components/
│   │   ├── hooks/
│   │   ├── store/
│   │   └── types.ts
│   ├── i18n/         # 国际化（包含组件）
│   │   ├── components/
│   │   ├── context/
│   │   └── ...
│   └── ai/           # AI 功能
└── shared/           # 共享代码（包含 Providers）
    ├── providers/
    ├── hooks/
    └── ...
```

## ✅ 优化后的优势

1. **功能模块完整**：每个功能模块包含所有相关代码（组件、逻辑、状态）
2. **职责清晰**：components/ 只保留基础 UI 组件
3. **组织一致**：所有功能模块采用相同的组织方式
4. **便于维护**：相关代码集中，易于查找和修改

