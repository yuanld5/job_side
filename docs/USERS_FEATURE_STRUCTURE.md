# Users 功能模块结构分析

## 📊 当前结构

```
src/domains/users/
└── services/
    └── userService.ts  (包含类型定义和服务方法)
```

## 🔍 与其他功能模块对比

### Chat 功能模块
```
src/features/chat/
├── hooks/
│   └── useChat.ts
├── store/
│   └── chatStore.ts
└── types.ts
```

### i18n 功能模块
```
src/features/i18n/
├── components/
│   └── LanguageSwitcher.tsx
├── context/
│   └── I18nContext.tsx
├── locales/
│   ├── en.ts
│   └── zh.ts
└── utils/
    └── getTranslation.ts
```

### AI 功能模块
```
src/features/ai/
├── agents/
├── llm/
├── services/
└── tools/
```

## ⚠️ 当前问题

1. **类型定义混在 Service 中** - 类型应该独立管理
2. **缺少 Hooks** - 如果需要在组件中使用，需要 React Hooks
3. **缺少组件** - 用户相关的 UI 组件没有地方放
4. **缺少状态管理** - 如果需要全局用户状态，需要 store

## ✅ 推荐结构

### 方案 1：完整功能模块（推荐）

```
src/domains/users/
├── types.ts              # 类型定义
├── services/
│   └── userService.ts   # 服务层（调用 API）
├── hooks/
│   └── useUsers.ts      # React Hook（可选）
├── components/          # 用户相关组件（可选）
│   └── UserList.tsx
└── store/               # 用户状态管理（可选）
    └── userStore.ts
```

### 方案 2：最小化结构（当前场景）

如果用户功能只是简单的 CRUD，可以保持简单：

```
src/domains/users/
├── types.ts              # 类型定义（提取出来）
└── services/
    └── userService.ts   # 服务层
```

## 🎯 建议

根据项目的实际需求：

1. **如果只是简单的 API 调用** → 方案 2（最小化）
2. **如果需要 React Hooks** → 添加 `hooks/`
3. **如果需要 UI 组件** → 添加 `components/`
4. **如果需要状态管理** → 添加 `store/`

## 📝 优化步骤

1. 提取类型定义到 `types.ts`
2. 根据实际需求添加其他目录
3. 保持与其他功能模块的一致性

