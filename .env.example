# Qwen API Configuration
# 使用 QWEN_API_KEY 或 DASHSCOPE_API_KEY 之一即可
QWEN_API_KEY=your_api_key_here
# or
DASHSCOPE_API_KEY=your_api_key_here

# Optional: Configure Qwen model
# 可选：配置 Qwen 模型名称（默认: qwen-turbo）
QWEN_MODEL=qwen-turbo

# Optional: Configure Qwen API base URL
# 可选：配置 Qwen API 基础 URL（默认: https://dashscope.aliyuncs.com/compatible-mode/v1）
QWEN_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1

# Optional: Use DashScope API directly
# 可选：直接使用 DashScope API（设置为 true 时使用 ChatAlibabaTongyi）
USE_DASHSCOPE=false

# Optional: Set application locale
# 可选：设置应用语言环境（zh 或 en，默认: zh）
LOCALE=zh

# Optional: Set log level
# 可选：设置日志级别（DEBUG, INFO, WARN, ERROR，默认: INFO）
LOG_LEVEL=INFO
